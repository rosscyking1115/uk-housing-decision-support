import { ScreenStub } from '@/components/screen-stub';

export default function CompareScreen() {
  return (
    <ScreenStub
      title="Compare"
      note="Up to four areas side by side, indicator by indicator — every cell stacks score, bar and raw figure so you compare across a row, never crown a column. Coming in the next build slice."
    />
  );
}
