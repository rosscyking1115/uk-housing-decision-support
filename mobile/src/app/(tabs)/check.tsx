import { ScreenStub } from '@/components/screen-stub';

export default function CheckScreen() {
  return (
    <ScreenStub
      title="Check a listing"
      note="Postcode + beds + asking price → the area it sits in, its five indicators, and where the price sits against the local median (stated plainly, never 'a good deal'). Coming in the next build slice."
    />
  );
}
