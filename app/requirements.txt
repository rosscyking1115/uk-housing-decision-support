# Slim runtime dependencies for the renter decision app ONLY (app/streamlit_app.py).
# Streamlit Cloud picks the requirements file in the entrypoint's directory
# (this one) over the project-root requirements.txt, skipping dbt + its deps.
#
# Pins are deliberately loose (>=) so that whatever Python version Streamlit
# Cloud uses (currently Python 3.14), the resolver finds a version with prebuilt
# wheels. Strict == pins (e.g. numpy==2.1.3 / pandas==2.2.3) have no cp314 wheels
# and forced a ~45-minute source build that hung the deploy.
streamlit>=1.57
duckdb>=1.4
pandas>=2.3
numpy>=2.1
requests>=2.32
